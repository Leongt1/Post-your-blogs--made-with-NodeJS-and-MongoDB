<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('includes/head', { title: 'All Posts' }) %>
  <link rel="stylesheet" href="/styles/posts.css">
</head>

<body>
  <%- include('includes/header') %>
  <main id="all-posts">
    <h1>All Posts</h1>
    <!-- if there are no posts to show -->
    <% if(!posts || posts.length === 0) { %>
      <p>No posts found - maybe start creating some new posts</p>
      <a class="btn" href="/new-post">Create a new Post</a>
    <% }else { %>
      <ol id="posts-list">
        <!-- to loop through the posts array -->
        <% for(const post of posts) { %> 
        <li>
          <!-- to include the external file with the post values -->
          <%- include('includes/post-item', {post: post}) %>
        </li>
        <% } %>
      </ol>
    <% } %>
  </main> 
</body>

</html>